/**
 * @file Defines a function that creates and sends a request to the Canvas Creation Service
 */

 const http = require('http');

 const sendRequest = (endpoint, ) => {

 };

 module.exports = {
   sendRequest,
 };
